const configNameToAlias = {
    photograph: 'Фотограф'
}

const config = {
    photograph: {
        components: [
            {
                type: 'TextInput (название компонента)',
                name: '<имя поля для валидации>',
                props: {
                    // Все необходимые пропсы компонента
                }
            }
        ],
        text: '<Весь текст страницы>'
    }
}
